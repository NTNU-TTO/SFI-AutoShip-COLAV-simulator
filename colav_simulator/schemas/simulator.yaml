scenario_files:
  required: True
  type: list
  minlength: 0
  schema:
    type: string

save_scenario_results:
  required: True
  type: boolean
visualize:
  required: True
  type: boolean
verbose:
  required: True
  type: boolean

ais_data_column_format:
  required: True
  type: list
  allowed: ["mmsi", "lon", "lat", "date_time_utc", "sog", "cog", "true_heading", "nav_status", "message_nr", "source"]
  minlength: 10
